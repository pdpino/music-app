<h1>Editing url</h1>

<%= form_for(@song, url: { action: "update_youtube" }) do |f| %>

  <div>
    <%= f.label :youtube_url, "URL" %>
    <%= f.text_field :youtube_url %>
  </div>

  <div>
    <%= f.submit "Guardar" %>
  </div>
<% end %>
