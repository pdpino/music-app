<h1> <%= title %> </h1>

<%= form_for(genre) do |f| %>

  <%= render '_errors/snippet', errors: genre.errors %>

  <div>
    <%= f.label :name, "Nombre" %>
    <%= f.text_field :name %>
  </div>

  <div>
    <%= f.submit "Guardar" %>
  </div>
<% end %>

<%= link_to 'Volver', back_path %>
