<h1>Género</h1>

<p>
  <strong>Nombre:</strong>
  <%= @genre.name %>
</p>

<% if @has_permission %>
  <%= link_to 'Editar', edit_genre_path(@genre) %>
  <%= link_to 'Eliminar', @genre, method: :delete, data: { confirm: 'Está seguro?' } %>
  <br> <!-- HACK: use CSS instead of this -->
<% end %>

<%= link_to 'Volver', genres_path %>
